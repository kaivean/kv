/**
 * @file  uploader
 * @author wukaifang(wukaifang@baidu.com)
 */

define(function (require) {
    var lib = require('./lib');

    var uploader = {};

    var defaultOpts = {
        id: '',
        action: '',
        enctype: '',
        target: '',
        field: [
            {
                
            }
        ]
    }

    return uploader;
});
