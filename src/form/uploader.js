/**
 * @file  uploader
 * @author kaivean(kaivean@outlook.com)
 */

define(function (require) {
    var lib = require('./lib');

    var uploader = {};

    var defaultOpts = {
        id: '',
        action: '',
        enctype: '',
        target: '',
        field: [
            {
                
            }
        ]
    }

    return uploader;
});
